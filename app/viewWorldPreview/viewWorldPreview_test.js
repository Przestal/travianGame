'use strict';

describe('myApp.viewWorldPreview module', function() {

  beforeEach(module('myApp.viewWorldPreview'));

  describe('viewWorldPreview controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewWorldPreviewCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});