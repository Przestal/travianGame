'use strict';

describe('myApp.viewAddWorld module', function() {

  beforeEach(module('myApp.viewAddWorld'));

  describe('viewAddWorld controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewAddWorldCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});